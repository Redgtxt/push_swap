#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H
#include "libft/libft.h"
#include <stdio.h>
#include <limits.h>

typedef	struct s_node
{
  int data;
  struct node *next;
}	t_node;

void write_error(void);

#endif

